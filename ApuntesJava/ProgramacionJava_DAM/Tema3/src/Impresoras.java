/*2.-Crear la clase Impresoras con los atributos: marca, Velocidad de Impresión(páginas por minuto),  peso (con decimales),
color (este atributo tiene 1 solo carácter: r (rojo) , v(verde),…),  buffer de memoria de la impresora (nº de MB), 
interfaz de conexión (USB, Wifi, …).

En el constructor teclear los datos correspondientes a los atributos anteriores.

Codificar un método que controle el color de la impresora. (La impresora sólo existe en los siguientes colores:  r, v, a, b, n), 
si el color tecleado no es alguno de estos visualizar un mensaje y teclear el color correcto.
Codificar otro método que visualice con un formato cuidado los datos de la impresora.

Codificar un método que visualice  marca y color.
- color se debe visualizar con el nombre de color completo: rojo, verde,… (implementar  éste método con if).

Codificar un método que visualice marca, peso y color.
- color se debe visualizar con el nombre de color completo: rojo, verde,… (implementar  éste método con switch).

Crear 2 objetos pertenecientes a la clase Impresoras e invocar(llamar) a cada uno de los métodos  creados.
*/
import java.util.Scanner;
public class Impresoras {

	String marca,velDeImpr,buffer,conex;	
	float peso;
	char color;
	
public static void main(String[]args){
	
}
}
